{
  "name": "pricing",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "with-env": "dotenv -e ../../.env --",
    "dev": "pnpm with-env next dev --port 3005 --turbopack",
    "build": "pnpm with-env next build",
    "start": "NODE_ENV=production pnpm with-env next start --port 3005",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/shared": "workspace:*"
  },
  "devDependencies": {}
}
