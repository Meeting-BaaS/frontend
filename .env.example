# Environment Configuration for auth application

# The prefix for session cookies.
AUTH_COOKIE_PREFIX=meeting-baas

# Better auth secret. This is used to sign the session cookie. Generate it here - https://www.better-auth.com/docs/installation#set-environment-variables
BETTER_AUTH_SECRET=""

# Database URL. Used to connect to database for authentication (creation, validation of accounts)
DATABASE_URL=""

# JWT Encoding secret. For backward compatibility, this app also generates a JWT which is used by backend services
# The value needs to be same as that provided in the backend
USER_ENCODING_KEY=""

# Bots API Key secret. This is used in creation of the api key. Generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
BOTS_API_KEY_SECRET=""

## OAuth Credentials for different OAuth logging in. Only Google and GitHub are enabled in the code, but they can be set optionally as per requirement.
# Google OAuth configuration. Documentation - https://www.better-auth.com/docs/authentication/google
GOOGLE_ID=""
GOOGLE_SECRET=""

# Microsoft OAuth configuration (requires tenant ID for organizational accounts), Documentation - https://www.better-auth.com/docs/authentication/microsoft
MICROSOFT_ID=""
MICROSOFT_SECRET=""
MICROSOFT_TENANT_ID=""

# Zoom OAuth configuration. Documentation - https://www.better-auth.com/docs/authentication/zoom
ZOOM_ID=""
ZOOM_SECRET=""

# GitHub OAuth configuration. Documentation - https://www.better-auth.com/docs/authentication/github
GITHUB_ID=""
GITHUB_SECRET=""

# GitLab OAuth configuration. Documentation - https://www.better-auth.com/docs/authentication/gitlab
GITLAB_ID=""
GITLAB_SECRET=""

# Application domain/subdomain (e.g., ".meetingbaas.com" without protocol). This is used to set the domain for the cookie.
DOMAIN=""

# Comma-separated list of trusted origins for CORS (e.g., "https://meetingbaas.com,https://logs.meetingbaas.com") with no trailing forward slash
TRUSTED_ORIGINS=""

# For prod, the environment key is expected to be blank. 
# For non-prod environments, NEXT_PUBLIC_ENVIRONMENT should be set to a domain prefix 
# (e.g., "pre-prod-", "dev-") WITHOUT a trailing dot
# This is used to construct the URLs for applications to be displayed on the homepage in conjunction with NEXT_PUBLIC_BASE_DOMAIN
# For example, https://logs.pre-prod-meetingbaas.com
NEXT_PUBLIC_ENVIRONMENT=pre-prod-

# Meeting BaaS base domain for external app URLs (Without protocol or leading dot). This is not the same as domain key above as it is used only for URL creation
NEXT_PUBLIC_BASE_DOMAIN=meetingbaas.com

# Meeting BaaS API Server Base URL. This is used for Server API calls, and is required to be configured
API_SERVER_BASEURL=

# Meeting BaaS Email Service Base URL. This can be configured optionally. 
# It is used to send emails for account verification, password resets. If this is not set, email/password authentication may not work
EMAIL_API_SERVER_BASEURL=

# API Key for Email Service. If EMAIL_API_SERVER_BASEURL is provided, then this is required. 
EMAIL_SERVICE_API_KEY=

# Meeting BaaS Redis Server URL for MCP connections. Optional key, can be omitted if MCP servers don't need Redis to be configured
NEXT_PUBLIC_REDIS_URL_MCP=

# Support email, to be displayed on error boundary page, e.g. support@meetingbaas.com
NEXT_PUBLIC_SUPPORT_EMAIL=support@meetingbaas.com

# AI Chat App configuration. When a user first signs up, a chat is created for them in the AI Chat App. 
# If not needed, please set the flag to false
ENABLE_AI_CHAT_INTEGRATION=false
# AI Chat Database URL (PostgreSQL). Used to query user chat information for the user from ai-chatbot_chat table
AI_CHAT_DB=
# When empty, hardcoded value is used, with NEXT_PUBLIC_BASE_DOMAIN appended with ENVIRONMENT (defined above)
AI_CHAT_APP_URL=
